apiVersion: kubeflow.org/v1
kind: TFJob
metadata:
  namespace: dljobs
  name: cnn-l
spec:
  runPolicy:
    cleanPodPolicy: None
  tfReplicaSpecs:
    Worker:
      replicas: 2
      restartPolicy: Never
      template:
        spec:
          containers:
            - name: tensorflow
              command: ["python", "CNN-L.py"]
              args: ["--batch_size", "16", "--learning_rate", "0.0001", "--epochs", "100", "--workload_name", "cnn-l", "--data_dir", "/train"]
              image: 10.170.23.190:8081/dljob/cnnl:v1.0
              volumeMounts:
                - mountPath: /train
                  name: training
              resources:
                limits:
                  aliyun.com/gpu-mem: 1
          volumes:
            - name: training
              persistentVolumeClaim:
                claimName: test-pvc
